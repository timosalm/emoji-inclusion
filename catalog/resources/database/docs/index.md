# Database for Inclusion app

PostgreSQL is provisioned via Helm Chart in the TAP Kubernetes cluster. 
The image is trusted, comes from VMware Application Catalog and is stored in our private registry.
